import React from "react";

type MyProps = {
  // using `interface` is also ok
  message: string;
};
type MyState = {
  count: number; // like this
};

export default class Department extends React.Component<MyProps, MyState> {
  state = {
    count: 0
  };

  render() {
    const { count } = this.state;

    return <h1>haha{count + 1}</h1>;
  }
}
